services:
  - type: web
    name: ragdoll-hit-game
    env: node
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    plan: free